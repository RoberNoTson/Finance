# Makefile for cleaning only
#
# 
#CC=/usr/bin/gcc
#CFLAGS=-Wall -O2 -ffast-math
#MYSQL_FLAGS=`mysql_config --include --libs`
#CURL_FLAGS=`curl-config --libs`
#LDFLAGS=-lpthread $(MYSQL_FLAGS) $(CURL_FLAGS)

clean:
	rm -f *.o
	rm -f *~
	rm -f asdf
